{
  "name": "autoux",
  "version": "1.0.0",
  "description": "AutoUX - AI-Powered UX Analysis System with Web3 Verification",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "web3"
  ],
  "scripts": {
    "dev": "concurrently -n BACK,FRONT -c auto,auto \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "npm run dev",
    "build": "npm --prefix frontend run build",
    "build:backend": "echo 'Backend does not require build step'",
    "build:frontend": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "prod:backend": "npm --prefix backend run start",
    "prod": "concurrently -n BACK,FRONT -c auto,auto \"npm run prod:backend\" \"npm run preview\"",
    "test": "npm --prefix backend test && npm --prefix frontend test",
    "test:backend": "npm --prefix backend test",
    "test:frontend": "npm --prefix frontend test",
    "lint": "npm --prefix frontend run lint",
    "clean": "npm --prefix frontend run clean",
    "security-check": "node security-check.js",
    "audit": "npm audit && npm --prefix backend audit && npm --prefix frontend audit"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
